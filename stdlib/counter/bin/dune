(executables
 (names counter)
 (libraries command_unix_for_opam bopkit_counter)
 (preprocess
  (pps ppx_jane ppx_js_style -check-doc-comments)))

(install
 (section
  (site
   (bopkit stdbin)))
 (files counter.exe))
