/**
 * There is some uncertainty at the moment regarding duplicated blocks,
 * and this may change. At the moment, previous blocks are discarded and
 * not checked. This feels a bit surprising. TBD
 */
Main() = c
where
  // There are several issues with this block, but since it is discarded,
  // it does not raise any error or warning.
  a = id(a);
end where;

Main() = ()
where
end where;
